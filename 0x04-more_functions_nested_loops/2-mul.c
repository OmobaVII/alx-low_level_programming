#include "main.h"

/**
 * mul - Used to multiply two integers
 * @a: for the first number
 * @b: for the second number
 * Return: multiplication of both numbers (Success)
 */

int mul(int a, int b)
{
		return (a * b);
}
